# Docs Workspace OS Desk

Live pod id: `019daa51-ae00-7387-9942-3f50ef6a9d06`

Use this when the workspace is centered on page hierarchy, pod files, and assistant-guided knowledge retrieval.

Backed resources:

- `pages` table
- `/workspace` folder tree
- `knowledge-guide` assistant

Primary blocks:

- `lemma-page-tree`
- `lemma-document-workspace`
- `lemma-file-browser`
- `lemma-breadcrumbs`
- `lemma-assistant-experience`

Copy [desk.tsx](./desk.tsx) into a desk scaffold and set `VITE_LEMMA_POD_ID` from [.env.example](./.env.example).
